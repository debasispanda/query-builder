name: Deploy Storybook

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup npm
        run: npm install -g npm@10.9.2

      - name: Install dependencies
        run: npm install --include=optional

      - name: Install Linux native bindings
        run: |
          npm install --no-save \
            "@oxc-resolver/binding-linux-x64-gnu@$(node -p \"require('./node_modules/oxc-resolver/package.json').optionalDependencies['@oxc-resolver/binding-linux-x64-gnu']\")" \
            "@oxlint/binding-linux-x64-gnu@$(node -p \"require('./node_modules/oxlint/package.json').optionalDependencies['@oxlint/binding-linux-x64-gnu']\")"

      - name: Build Storybook
        run: npm run build-storybook -w docs

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/docs/storybook-static

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4